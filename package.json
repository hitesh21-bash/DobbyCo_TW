{
  "name": "dobbyco",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend && cd frontend && npm install && npm install --save-dev vite",
    "build": "cd frontend && npm run build",
    "start": "npm run start --prefix backend"
  },
  "engines": {
    "node": "18.x"
  }
}